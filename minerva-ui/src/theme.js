import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    type: "dark",
    background: {
      default: "#0a0a32",
    },
    text: {
      primary: "ivory",
      secondary: "gold",
    },
  },
});

export default theme;
